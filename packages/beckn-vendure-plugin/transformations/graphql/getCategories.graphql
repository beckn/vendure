query GetCategories {
    facets(options: { filter: { name: { eq: "category" } } }) {
        totalItems
        items {
            id
            values {
                id
                facetId
                name
                code
            }
        }
    }
}
