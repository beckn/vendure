query GetBecknTransactionFromVendureAuthToken($vendureAuthToken: String!) {
    getBecknTransactionFromVendureAuthToken(vendureAuthToken: $vendureAuthToken) {
        id
        createdAt
        updatedAt
        becknTransactionId
        vendureAuthToken
    }
}
