query GetBecknTransaction($becknTransactionId: String!) {
    getBecknTransaction(becknTransactionId: $becknTransactionId) {
        id
        createdAt
        updatedAt
        becknTransactionId
        vendureAuthToken
    }
}
