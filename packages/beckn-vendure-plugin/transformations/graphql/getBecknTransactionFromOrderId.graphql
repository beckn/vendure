query GetBecknTransactionFromOrderId($orderId: String!) {
    getBecknTransactionFromOrderId(orderId: $orderId) {
        id
        createdAt
        updatedAt
        becknTransactionId
        vendureAuthToken
    }
}
